{
  "name": "ai-career-showcase",
  "version": "1.0.0",
  "private": true,
  "description": "AI 职业规划小程序 · 交互式功能展示站",
  "scripts": {
    "dev": "esbuild src/main.ts --bundle --outfile=dist/main.js --servedir=. --serve=8080",
    "build": "esbuild src/main.ts --bundle --outfile=dist/main.js --minify",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
